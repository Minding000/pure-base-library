// The different streams should be properties of the application
//  initializer by a native call to a initializer function that takes them as parameters
// Then you should be able to import the property object in other files

CLI object {
	
	to write(text: String) {
		
	}

	to print(text: String) {
		write("$text\n")
	}
}