referencing Pure

abstract Identifiable class {
	//overriding native gettable computed hashCode: Int

	native overriding gettable computed stringRepresentation: String

	overriding operator ==(other: Any?): Bool {
		return this === other
	}
	overriding operator !=(other: Any?): Bool {
		return !(this == other)
	}
	native operator ===(other: Any?): Bool
	operator !==(other: Any?): Bool {
		return !(this === other)
	}
}
