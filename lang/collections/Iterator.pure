referencing Pure

Iterator class {
	containing Element
	val dataSource: <Element>Collection
	val position: Int

	init(dataSource)
	
	val current: Element
		gets dataSource[position]

	operator ++ {
		position++
	}
}